#include <cstdio>
#include <cstring>
#include <cstdlib>
#include <algorithm>
using namespace std;
typedef long long ll;

int main() {
    scanf("%d", &n,&m);
    for (i = 1; i <= m; i++) {
        scanf("%d %d &d",&a,&b,&c );
        cur.v=b;
        cur.w=c;
    }
    system("pause");
    return 0;
}